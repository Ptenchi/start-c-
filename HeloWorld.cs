using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HeloWorld : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("Helo, world");
        //  выводит текст в кансоль
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
